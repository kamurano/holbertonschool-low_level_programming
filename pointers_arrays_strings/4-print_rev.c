#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * print_rev - func
 * @s: variable
*/
void print_rev(char *s)
{
	char *p = s;
	
	while(*p != '\0')
		p++;

	int = i;

	for (i = p; s[i] != s[0]; i--)
		_putchar s[i];
	_puthcar('\n')
}
